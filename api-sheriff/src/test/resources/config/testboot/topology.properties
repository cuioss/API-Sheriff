# Topology aliases for the @QuarkusTest boot configuration.
#
# UPSTREAM points at the MockWebServer that ProxyRouteTest starts on the fixed
# port 19191. The proxy edge sources this upstream from the assembled RouteTable
# (endpoints/proxy.yaml -> UPSTREAM), no longer from a static ProxyConfiguration.
UPSTREAM=http://localhost:19191
