# Topology alias -> upstream URL mappings.
# Aliases match [A-Z][A-Z0-9_]*; TOPOLOGY_<ALIAS> environment variables override
# these file values at boot. Consumed by the topology resolver (Deliverable 5).
ORDERS=https://orders.internal:8443/api
USERS=https://users.internal:8443
