# Topology aliases for the anchored fixture. The URLs are never dialed by the
# validator or the route-table builder — only parsed for alias resolution.
API=http://localhost:19191
BFF=http://localhost:19192
