<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>de.cuioss.sheriff.gateway</groupId>
        <artifactId>api-sheriff-parent</artifactId>
        <version>0.2.0-SNAPSHOT</version>
        <relativePath>../pom.xml</relativePath>
    </parent>

    <artifactId>api-sheriff</artifactId>
    <packaging>jar</packaging>
    <name>API Sheriff</name>
    <description>Unified Quarkus application module for API Sheriff gateway with security features</description>

    <properties>
        <maven.jar.plugin.automatic.module.name>de.cuioss.sheriff.gateway</maven.jar.plugin.automatic.module.name>
        <!-- The Quarkus entry point is not coverable, and this states that where Sonar can see it.
             Every path through ApiSheriffApplication.main either terminates the JVM (System.exit on
             the health-probe branch) or hands control to Quarkus.run, so no test can execute it; the
             same is true of run(), which only awaits shutdown. The JaCoCo <excludes> further down
             this POM already declares that intent for the local report, but a JaCoCo exclusion only
             withholds coverage DATA — Sonar keeps counting the lines and scores them as uncovered,
             which is what drove new_coverage to 0.0 on PR #230. Both mechanisms must name the class
             for the two to agree; keep them in step.
             This is a scope declaration, not a way to hide untested logic: the class is deliberately
             a shim carrying no logic of its own. The probe it routes to lives in HealthProbe, which
             is measured normally and is fully covered. Any logic added here rather than there is a
             defect in the split, not a reason to widen this exclusion. -->
        <sonar.coverage.exclusions>**/ApiSheriffApplication.java</sonar.coverage.exclusions>
    </properties>

    <dependencies>
        <!-- Core library dependencies -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
        </dependency>
        <dependency>
            <groupId>de.cuioss</groupId>
            <artifactId>cui-java-tools</artifactId>
        </dependency>
        <!-- cui-http: inbound security filtering (de.cuioss.http.security) and the
             forwarded-header resolver (de.cuioss.http.forwarded); version 2.1.0 managed
             by cui-java-bom's ${version.cui.http} -->
        <dependency>
            <groupId>de.cuioss</groupId>
            <artifactId>cui-http</artifactId>
        </dependency>

        <!-- Configuration management: YAML parsing + boot-time JSON Schema validation -->
        <dependency>
            <groupId>com.fasterxml.jackson.dataformat</groupId>
            <artifactId>jackson-dataformat-yaml</artifactId>
        </dependency>
        <dependency>
            <groupId>com.networknt</groupId>
            <artifactId>json-schema-validator</artifactId>
        </dependency>

        <!-- Quarkus dependencies -->
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-core</artifactId>
        </dependency>
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-arc</artifactId>
        </dependency>
        <!-- quarkus-resteasy: RETAINED after the Plan-04 data-plane cutover (the JAX-RS
             GatewayResource placeholder was removed; the public edge is now the Vert.x
             GatewayEdgeRoute). It stays because GatewayExceptionMapper is a JAX-RS @Provider
             (jakarta.ws.rs.ext.ExceptionMapper) rendering the RFC 9457 problem+json contract, and
             token-sheriff-client-quarkus registers its own JAX-RS RFC 9457 mapper — both require a
             JAX-RS runtime. Dropping it would also need explicit approval per the dependency rule. -->
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-resteasy</artifactId>
        </dependency>
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-resteasy-jackson</artifactId>
        </dependency>
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-smallrye-health</artifactId>
        </dependency>
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-micrometer</artifactId>
        </dependency>
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-micrometer-registry-prometheus</artifactId>
        </dependency>
        <!-- SmallRye Fault Tolerance: programmatic Guard/TypedGuard + CircuitBreakerMaintenance
             for the per-route resilience guard held on RouteRuntime (Quarkus-BOM-managed) -->
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-smallrye-fault-tolerance</artifactId>
        </dependency>

        <!-- Token Sheriff: client engine + Quarkus extensions (versions managed by token-sheriff-bom) -->
        <dependency>
            <groupId>de.cuioss.sheriff.token</groupId>
            <artifactId>token-sheriff-client</artifactId>
        </dependency>
        <dependency>
            <groupId>de.cuioss.sheriff.token</groupId>
            <artifactId>token-sheriff-client-quarkus</artifactId>
        </dependency>
        <dependency>
            <groupId>de.cuioss.sheriff.token</groupId>
            <artifactId>token-sheriff-validation-quarkus</artifactId>
        </dependency>

        <!-- Test dependencies -->
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
        <!-- Awaitility: tiered, instrumented polling ceilings for the live-server edge/tls tests,
             replacing fixed wall-clock sleeps. Version-less on purpose: the artifact is BOM-managed
             for this reactor, exactly as benchmarks/pom.xml already declares it.
             HAMCREST CONSTRAINT, deliberate and load-bearing: Awaitility drags org.hamcrest in
             transitively and it is NOT excluded, because Awaitility's own ConditionFactory API
             references org.hamcrest.Matcher and excluding it yields NoClassDefFoundError at runtime.
             CLAUDE.md forbids Hamcrest in authored test code, so the constraint is honoured by
             import discipline rather than by exclusion: never import org.hamcrest.* anywhere in
             this module's tests. Use only untilAsserted(...) and until(Callable<Boolean>), both of
             which take JUnit assertions / plain Java predicates and touch no Hamcrest type. -->
        <dependency>
            <groupId>org.awaitility</groupId>
            <artifactId>awaitility</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-junit</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-jacoco</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>io.rest-assured</groupId>
            <artifactId>rest-assured</artifactId>
            <scope>test</scope>
        </dependency>
        <!-- CUI test dependencies -->
        <dependency>
            <groupId>de.cuioss.test</groupId>
            <artifactId>cui-test-mockwebserver-junit5</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>de.cuioss.test</groupId>
            <artifactId>cui-test-value-objects</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>de.cuioss.test</groupId>
            <artifactId>cui-test-generator</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>de.cuioss.test</groupId>
            <artifactId>cui-test-juli-logger</artifactId>
            <scope>test</scope>
        </dependency>
        <!-- cui-http attack databases (generators classifier) driving the adversarial
             pipeline tests; classifier variant is not BOM-managed, so the inherited
             ${version.cui.http} pins it to the same 2.1.0 as the main artifact -->
        <dependency>
            <groupId>de.cuioss</groupId>
            <artifactId>cui-http</artifactId>
            <version>${version.cui.http}</version>
            <classifier>generators</classifier>
            <scope>test</scope>
        </dependency>
        <!-- token-sheriff-validation generators (JWKS / key material for the offline bearer-token
             unit tests); the classifier variant is not BOM-managed, so pin it to the stack version
             explicitly, mirroring the cui-http generators dependency above -->
        <dependency>
            <groupId>de.cuioss.sheriff.token</groupId>
            <artifactId>token-sheriff-validation</artifactId>
            <version>${version.token-sheriff}</version>
            <classifier>generators</classifier>
            <scope>test</scope>
        </dependency>
        <!-- JJWT runtime for the token-sheriff-validation generators test-jar. TestTokenHolder
             (de.cuioss.sheriff.token.validation.test) uses Jwts.SIG (jjwt-api 0.12+) and needs
             jjwt-impl + jjwt-jackson on the TEST runtime classpath to sign/parse. token-sheriff-validation
             declares all three jjwt artifacts at test scope, so they do NOT transitively reach this
             consumer — a cold Maven resolution (CI, fresh env) therefore omits them and TestTokenHolder's
             static initializer fails with NoClassDefFoundError io/jsonwebtoken/Jwts$SIG. Declared here
             at test scope with the version inherited from token-sheriff-bom (${version.jjwt} = 0.13.0),
             so the jjwt line always matches the token-sheriff stack. -->
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-api</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-impl</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-jackson</artifactId>
            <scope>test</scope>
        </dependency>
        <!-- SmallRye Fault Tolerance STANDALONE SPI for the DispatchStageTest retry-gating unit tests.
             Those tests build a Guard programmatically via io.smallrye.faulttolerance.api.Guard.create()
             outside any CDI/Quarkus runtime; SpiAccess then ServiceLoader-resolves an
             io.smallrye.faulttolerance.api.Spi implementation. The compile-scope quarkus-smallrye-fault-tolerance
             pulls the CDI implementation (io.smallrye:smallrye-fault-tolerance), which registers its Spi via CDI
             beans, NOT via META-INF/services — so a plain JUnit run finds no Spi and Guard.create() fails with
             "Could not find implementation of io.smallrye.faulttolerance.api.Spi" (ExceptionInInitializerError /
             NoClassDefFoundError). smallrye-fault-tolerance-standalone supplies the non-CDI Spi these tests need.
             Test-scope only (production uses the CDI-wired Guard); version inherited from the Quarkus BOM,
             matching the 6.11.1 smallrye-fault-tolerance line already on the classpath. -->
        <dependency>
            <groupId>io.smallrye</groupId>
            <artifactId>smallrye-fault-tolerance-standalone</artifactId>
            <scope>test</scope>
        </dependency>
        <!-- ArchUnit: ADR-0005 framework-agnostic package-boundary gate (FrameworkAgnosticArchTest) -->
        <dependency>
            <groupId>com.tngtech.archunit</groupId>
            <artifactId>archunit</artifactId>
            <version>1.5.0</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>io.quarkus</groupId>
                <artifactId>quarkus-maven-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>io.smallrye</groupId>
                <artifactId>jandex-maven-plugin</artifactId>
            </plugin>
            <!-- JaCoCo Plugin for test coverage -->
            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
                <configuration>
                    <excludes>
                        <exclude>**/ApiSheriffApplication.class</exclude>
                    </excludes>
                </configuration>
                <executions>
                    <execution>
                        <id>default-prepare-agent</id>
                        <goals>
                            <goal>prepare-agent</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>default-report</id>
                        <phase>test</phase>
                        <goals>
                            <goal>report</goal>
                        </goals>
                        <configuration>
                            <formats>
                                <format>XML</format>
                                <format>HTML</format>
                            </formats>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <annotationProcessorPaths>
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                        </path>
                    </annotationProcessorPaths>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <configuration>
                    <!-- The D4 secrets rule requires every secret-classified config field to be a
                         bare ${VAR} reference — a literal is refused at load time — and a bare
                         reference naming an undefined variable aborts the boot. CookieModeBootTest
                         boots the gateway from a real YAML fixture, so its oidc.client_secret must
                         resolve from the environment. This is a throwaway test-only value that
                         authenticates to nothing: the boot fixture reaches no IdP (OIDC discovery is
                         lazy) and no network. -->
                    <environmentVariables>
                        <COOKIE_BOOT_CLIENT_SECRET>cookie-boot-test-secret</COOKIE_BOOT_CLIENT_SECRET>
                    </environmentVariables>
                </configuration>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <profile>
            <id>native</id>
            <properties>
                <quarkus.native.container-build>true</quarkus.native.container-build>
                <quarkus.native.enabled>true</quarkus.native.enabled>
                <!-- Build-time GraalVM knob: enables SSL support in the native image. It is fixed when
                     the image is built, so it is deliberately OUTSIDE the neutral runtime configuration
                     surface — gateway.yaml is read at startup and could never make this key take effect.
                     Bound here rather than in application.properties so it lives with the other
                     build-time native properties and is active exactly when it has meaning. See ADR-0025
                     for the boundary rule separating build-time knobs, deployment-bound ports/trust
                     material, and gateway.yaml-owned TLS policy. -->
                <quarkus.ssl.native>true</quarkus.ssl.native>
            </properties>
        </profile>

        <!-- Context-path build seam.

             The three context-path keys declared in src/main/resources/application.properties are
             BUILD-TIME keys, fixed at augmentation, so the matching environment variables are inert
             against an already-built image. These two profiles are the operator-facing seam that
             changes them, consumed at build time:

               ./mvnw verify -Dsheriff.context-path=/gw
               ./mvnw verify -Dsheriff.management-context-path=/admin

             Each profile maps its user property onto a quarkus.*-prefixed CARRIER key, which
             application.properties then reads through an expression default. Both halves of that
             sentence are load-bearing and were established by measurement:

               (i)  THE PREFIX. quarkus-maven-plugin forwards properties into augmentation through
                    getBuildSystemProperties(quarkusOnly), which keeps ONLY `quarkus.`-prefixed
                    names. A profile that set `sheriff.context-path` as the build property would be
                    filtered out entirely and never reach the config.
               (ii) THE CARRIER, rather than quarkus.http.root-path directly. Forwarded build-system
                    properties land at ordinal 100; src/main/resources/application.properties is
                    ordinal 250 and therefore WINS for every key it declares. Setting
                    quarkus.http.root-path here would consequently be overridden by the declaration
                    it is trying to change — measured, not inferred: with the direct mapping,
                    `package -Dsheriff.context-path=/gw` produced ZERO occurrences of `/gw` in
                    generated-bytecode.jar. application.properties instead declares
                    `quarkus.http.root-path=${quarkus.sheriff-context-path:/}`, so the 250-ordinal
                    declaration still wins but resolves the carrier from whatever source supplies
                    it — this profile at ordinal 100, or its own inline default when nothing does.

             Do NOT "simplify" these two profiles back to setting quarkus.http.root-path /
             quarkus.management.root-path directly. It reads more obvious, every build stays green,
             and the seam silently stops acting. The carrier keys are logged as unrecognized
             configuration at boot — Quarkus owns no such keys — and that cost is accepted
             deliberately; see the (e) block in application.properties.

             That a plain Maven property reaches augmentation at all is not speculative: the
             `native` profile above already binds quarkus.native.enabled and quarkus.ssl.native the
             same way, which is this repo's own proof of the forwarding path.

             Both profiles are INACTIVE by default, and that is load-bearing rather than incidental:
             at default no carrier is supplied, so the expression's own default is the acting value
             and the build ships the stock `/` and `/q`.

             MAVEN ACTIVATES A PROFILE BY PROPERTY FROM USER PROPERTIES ONLY — `-D` on the command
             line, or an <activeProfiles>/<properties> entry in settings.xml. It NEVER activates one
             from a POM <properties> entry, because POM properties are resolved after profile
             activation has already been decided. This is exactly why the downstream build parent
             (de.cuioss.sheriff.gateway:api-sheriff-build-parent) uses an UNCONDITIONAL property
             mapping rather than a profile: an inheriting POM sets its context path as a POM
             property, which could never activate a profile here. The two shapes differ for that
             reason and are not an inconsistency. -->
        <profile>
            <id>context-path</id>
            <activation>
                <property>
                    <name>sheriff.context-path</name>
                </property>
            </activation>
            <properties>
                <quarkus.sheriff-context-path>${sheriff.context-path}</quarkus.sheriff-context-path>
            </properties>
        </profile>
        <profile>
            <id>management-context-path</id>
            <activation>
                <property>
                    <name>sheriff.management-context-path</name>
                </property>
            </activation>
            <properties>
                <quarkus.sheriff-mgmt-path>${sheriff.management-context-path}</quarkus.sheriff-mgmt-path>
            </properties>
        </profile>
    </profiles>
</project>
