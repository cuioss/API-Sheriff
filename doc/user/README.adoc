= API Sheriff -- Operator Guide
:toc:
:toclevels: 2
:sectnums:

The *operator layer* of the API Sheriff documentation. These documents are written for the
people who *run* the gateway -- deploying the binary or container, authoring and operating
`gateway.yaml`, and keeping a running deployment healthy. They describe the gateway as a
finished artifact to be configured and operated, distinct from the
link:../README.adoc[design documentation] under `doc/` (which defines *what* is built) and the
link:../development/README.adoc[contributor guide] (which describes how to *build* it).

This tree is seeded here and grows as operator-facing material lands; the 1.0 operator
configuration guide (link:../plan/09-release-readiness.adoc[plan 09]) is authored into this
layer.

== Contents

[cols="1,3"]
|===
| Document | Purpose

| link:../configuration.adoc[Configuration Reference]
| The authoritative reference for every `gateway.yaml` key -- routing, anchors and their
  required `type`/`access` axes, security filtering, TLS, and per-route limits. It carries a
  *dual role*: the operator-facing YAML reference and a design document (see the
  link:../README.adoc[design documentation index]). Operators read it as the configuration
  contract; it stays under `doc/` and is not relocated here.

| link:protocol-routes.adoc[Protocol Routes -- WebSocket and gRPC]
| A task-oriented guide for configuring `protocol: websocket` and `protocol: grpc` routes --
  setting `allowed_origins`, tuning `websocket.idle_timeout_seconds`, and authoring a gRPC route.
  Links to the Configuration Reference for the field contract and to the Architecture document
  for the gRPC error mapping.

| link:tls-edge.adoc[TLS Edge -- Policy, Passthrough SNI, mTLS and the Management Interface]
| Configuring the TLS policy keys (`tls.min_version`, `tls.cipher_suites`, `tls.alpn`) and their
  terminated-listener scope limit; `tls.passthrough_sni` (relay selected hostnames at L4 without
  terminating) and `tls.mtls` (require-and-verify a client certificate on terminated connections),
  plus the emergency trust-root-replacement blast radius every operator must understand before
  relying on mTLS; and the management interface -- HTTPS by default, the single-port constraint, the
  two doors onto the plain-HTTP opt-out, and why `quarkus.management.enabled=false` is not one of
  them. Links to the Configuration Reference for the field contract.

| link:bff-session.adoc[Server-Session BFF -- Operator Setup]
| A task-oriented guide for standing up the server-session BFF (Variant 2): registering the OIDC
  confidential client, protecting a route with `require: session`, the gateway-owned reserved
  paths, serving a curated user-info view, CSRF for a cross-origin SPA, and operating the in-memory
  session store. Links to the Configuration Reference for the `oidc` key contract.

| link:bff-cookie.adoc[Cookie-Based BFF -- Operator Setup]
| A task-oriented guide for standing up the cookie-based BFF (Variant 3): registering the OIDC
  confidential client, selecting `session.mode: cookie`, the two key-material modes and their
  trade-off, decrypt-only key rotation, the cookie size budget, what IdP-driven destruction cannot
  do in this variant, the login-leg session affinity, and the forced re-login on a sealed-cookie
  format bump. Links to the Configuration Reference for the `oidc` key contract.

| link:demo-client.adoc[Demo Client -- the BFF Integration Sample]
| The copy-this sample for putting a browser application behind a BFF gateway: the browser-facing
  contract of the reserved `/auth` paths, the four identity-disclosure states the `claims` parameter
  selects (including the `403` an operator-disallowed claim earns), the same-origin `returnUrl` rule
  and its silent-fallback behaviour, the `Cache-Control: no-store` guarantee, the asset-anchor
  configuration that lets the gateway serve the bundle itself, and why `oidc.logout.final_redirect`
  must name a concrete file rather than `/`.

| link:environment-variable-overrides.adoc[Environment-Variable Overrides]
| Which knobs the deployment owns and which `gateway.yaml` owns: the three-way classification
  (policy / deployment-bound / build-time), the precedence rule and why it has no exceptions, the
  enumerated deployment-bound variables (ports, certificate and trust material, the management TLS
  selection), and an audit of where else the codebase offers a secure default with an override
  route. Read it before assuming an environment variable can change a given behaviour.

| link:container-image.adoc[Container Image -- Pulling, Running and Verifying]
| The operator layer for the published image at `ghcr.io/cuioss/api-sheriff`: the two-tag scheme and
  why no `:latest` exists, a `docker run` that reflects what the artifact actually requires (a
  mounted configuration directory, deployment-supplied certificate material, and the
  `QUARKUS_HTTP_SSL_PORT` move under `tls.passthrough_sni`), what the SBOM and the HIGH+CRITICAL
  vulnerability scan do and do not guarantee, and the complete `cosign verify` recipe with both
  mandatory flags. Links to the release process for the artifact policy.
|===

== Scope of This Layer

Documents in `doc/user/` cover:

* *Deployment* -- running the native binary and the production container image, and the
  configuration mounted into them.
* *Configuration operation* -- authoring `gateway.yaml`, the anchor `type`/`access` axes, the
  asset terminal action, and the access-to-auth rules enforced at boot.
* *Running the gateway* -- health, metrics, and the operational signals a deployment exposes.

Design rationale for these features lives in the link:../README.adoc[design documentation] and
the link:../adr/0007-anchor-scoped-policy.adoc[Architecture Decision Records]; this layer
describes how to *use* them, never why they were chosen.

== Inbound Filter Modes (`security_defaults.profile`)

The `profile` knob selects the gateway's inbound-validation mode. It is declarable gateway-wide on
`security_defaults` and per route or anchor on `security_filter`. Field-level contract and the
resolution chain live in
link:../configuration.adoc#_security_defaults[Configuration Reference -- `security_defaults`]; this
section is the operator's decision guide.

=== The three modes

[cols="1,2,3"]
|===
| Mode | Choose it when | What you get

| `strict`
| Default choice. Any route serving untrusted clients, and every authenticated or BFF surface.
| The tightest caps: 1 MiB body, 20 query parameters, 1024-character header values, parameter values
  and paths, and suspicious-pattern rejection on. The 1024-character figure is the *preset* cap;
  `Cookie` / `Set-Cookie` values are the one exception -- a cookie-mode BFF gateway validates them
  pre-route against a raised, gateway-wide cap derived from `oidc.session.max_cookie_size`. See
  link:../configuration.adoc#_oidc[Configuration Reference -- `oidc.session.max_cookie_size`].

| `lenient`
| A trusted internal caller legitimately needs headroom -- long tokens in headers, many query
  parameters, larger bodies -- and you have decided that traffic is trustworthy.
| Relaxed caps (10 MiB body, 8192-character header values, no suspicious-pattern rejection). Null
  bytes are *still* refused and path traversal is *still* blocked -- no mode permits either.

| `minimal`
| A specific route receives url-parameter values the validator rejects but your application requires
  -- a path-shaped callback target, an opaque token containing separators. Prefer it *per route*: a
  gateway-wide `security_defaults.profile: minimal` is accepted only while no authenticated or BFF
  route inherits it, because the boot validator refuses the mode on those routes.
| Everything `strict` enforces *except* two things: the url-parameter name/value validation and the
  per-route pipeline re-run. See "What `minimal` does not turn off" below -- it is a partial disable,
  not an off switch.
|===

=== `minimal` is a partial disable, not an off switch

Setting `profile: minimal` does *not* stop the gateway defending the route. It turns off exactly two
things -- the url-parameter name and value validation, and the per-route pipeline re-run -- and
nothing else. In particular, a `minimal` route **still**:

* enforces `max_body_bytes`, so an over-cap body is still rejected `413`;
* enforces `allowed_paths`, so a path outside the allowlist is still rejected `400`;
* runs the full pre-route floor, so an encoded path separator (`%2F`), an over-cap query-parameter
  count, and an oversized header value are all still rejected `400`;
* enforces authentication, the verb gate, CSRF, and the forward-policy allowlists.

If you set `minimal` expecting the body cap or the path allowlist to stop applying, that is the
misreading to correct: the mode is the *least* validation the gateway performs, not none of it. The
normative closed list of what `minimal` disables is in
link:../configuration.adoc#_the_minimal_mode[Configuration Reference -- The `minimal` mode].

=== `minimal` is refused at boot on authenticated and BFF routes

The gateway *fails to start* if a route whose effective profile resolves to `minimal` is either

* effectively authenticated -- its resolved auth posture requires a bearer token or a session, or
* under a `type: bff` anchor.

This includes the indirect case: a gateway-wide `security_defaults.profile: minimal` that reaches such a
route through the fallback chain is refused exactly as a per-route declaration would be. It also
includes the subtle case: a route that adds its own `auth.require: bearer` under an `access: public`
anchor *is* authenticated in effect, so it is refused even though its anchor still reads `public`.

The boot error names the route, which dimension refused it, and the remedy. Fix it by giving that
route an explicit `profile: strict` (or `lenient`), or by moving the `minimal` declaration to the
specific public route that needs it.

=== An unrecognized `profile` value is a hard boot failure

The mode set is exactly `strict`, `lenient`, `minimal`. Any other value -- including the *removed*
`default` and the former `none`, now spelled `minimal` -- makes the gateway *refuse to start*. There
is no fallback and no warning-and-continue.

[IMPORTANT]
====
*Migration from `default`.* The `default` value has been removed. Replace it with `strict`:

[source,yaml]
----
security_defaults:
  profile: strict     # was: default
----

Choose `lenient` instead if you need to keep the looser caps `default` happened to provide.
====

[IMPORTANT]
====
*Migration from `none`.* The mode formerly spelled `none` is now spelled `minimal`. The behaviour is
unchanged -- only the name is -- but the old spelling is *not* accepted: a `gateway.yaml` still
declaring `profile: none` *fails to boot* with a value-range violation, at every site the key appears
(`security_defaults`, an anchor's `security_filter`, a route's `security_filter`).

*Remedy: rename the value.* One line per declaration site:

[source,yaml]
----
security_defaults:
  profile: minimal    # was: none
----

`auth.require: none` is a *different* key in a different value space and is *not* affected.
====

=== An omitted `security_defaults` block now means `strict`

*This is a tightening relative to previous releases.* A `gateway.yaml` that declares no
`security_defaults` block -- or declares one without `profile` -- now resolves to `strict`, where it
previously resolved to a looser incidental baseline.

*Symptom to look for after upgrading:* requests that previously succeeded start returning `400` --
or `413` when it is the body cap that dropped. The caps that dropped are:

[cols="2,1,1"]
|===
| Cap | Previously | Now (`strict`)

| Request body | 5 MiB | 1 MiB
| Query-parameter count | 100 | 20
| Header value length | 2048 chars | 1024 chars
| Parameter value length | 2048 chars | 1024 chars
| Path length | 4096 chars | 1024 chars
| Suspicious-pattern rejection | off | *on*
|===

If a previously-working request now returns `400` -- or `413` on an over-cap body -- check it against
that table first. Two remedies, in order of preference:

. *Raise only the cap you need*, on the specific route that needs it -- e.g.
  `security_filter: { max_body_bytes: 5242880 }`. This keeps the strict posture everywhere else.
. *Restore the looser baseline explicitly* with `security_defaults: { profile: lenient }`. This is a
  gateway-wide loosening; prefer option 1 unless you genuinely want it everywhere.

Declaring the value you want -- rather than relying on the omitted-block default -- is the durable
fix either way.

== Admitting a Body on `GET` (`security_defaults.allow_get_with_content_length_body`)

Some upstream APIs genuinely take a request body on `GET` -- Elasticsearch's `_search` is the
common one. By default API Sheriff rejects that request `400` at the anti-smuggling framing gate,
because a body on an otherwise-bodyless method is a request-smuggling shape. This knob admits the
legitimate case without disabling the defence. Field-level contract lives in
link:../configuration.adoc#_security_defaults[Configuration Reference -- `security_defaults`]; this
section is the operator's decision guide.

=== Symptom and fix

*Symptom:* a `GET` that carries a JSON body is rejected `400` with a framing violation, and the
upstream never sees it.

*Fix:* enable the opt-in gateway-wide:

[source,yaml]
----
security_defaults:
  profile: strict
  allow_get_with_content_length_body: true
----

The key is gateway-wide, not per-route, because the framing gate runs before route selection --
there is no route resolved yet to scope it to. Omit the key (or set `false`) and every framing
rejection behaves exactly as it did before the key existed.

*Decide it as a gateway-wide posture change, not an upstream-local one.* Since it cannot be scoped,
enabling it for the one upstream that needs a `GET` body also admits a `Content-Length`-framed `GET`
body on every other route -- your `require: bearer` routes and every route under a `type: bff`
anchor included. Content on `GET` has no defined semantics (RFC 9110 section 9.3.1), so the risk on
those routes is a cache that keys on the URL and ignores the body, or a component that authorizes on
the URL while the application reads the body. Leave the key `false` unless every route on the
gateway can tolerate a `GET` body.

=== What it does *not* turn off

Enabling it relaxes one rejection leg, and only for `GET`: the declared-`Content-Length` leg.
Everything else the framing gate enforces still applies. In particular, a `GET` is **still**
rejected `400` when it carries:

* a body with no declared `Content-Length` -- such a body is not `Content-Length`-framed, so it
  falls outside the single leg this key relaxes.
* `Transfer-Encoding` -- *unconditionally, and no configuration relaxes it.* Chunked framing on an
  otherwise-bodyless method is the exact shape the smuggling defences exist to constrain. This costs
  the use case nothing: an Elasticsearch client sends a `Content-Length`-framed body, never a chunked
  one, so the relaxation is drawn where the security value is rather than where the convenience is.
* both `Content-Length` and `Transfer-Encoding` -- the CL+TE desync primer, checked before this leg.
* more than one `Content-Length` header, or a comma-separated `Content-Length` value.
* a `Connection` header naming a framing or trust header (the hop-by-hop strip vector).

And `HEAD` is unaffected on every leg -- a body on `HEAD` stays rejected whatever this key says.

If you set this expecting `GET` framing checks to stop applying generally, that is the misreading to
correct: it admits one specific, well-framed shape and nothing else.

== Asset Content Types (`asset_defaults.content_types`)

The gateway -- never the backing directory or upstream -- decides the `Content-Type` of every asset
it serves, from its own built-in extension map. An extension the map does not carry is served as
`application/octet-stream`, which browsers download instead of rendering. `asset_defaults` is how
you teach the gateway that extension. Field-level contract lives in
link:../configuration.adoc#_asset_defaults[Configuration Reference -- `asset_defaults`]; this
section is the operator's decision guide.

=== Symptom and fix

*Symptom:* a file served through an `type: asset` anchor downloads instead of rendering, and the
response carries `Content-Type: application/octet-stream`.

*Fix:* declare its extension, gateway-wide:

[source,yaml]
----
asset_defaults:
  content_types:
    webmanifest: application/manifest+json
    avif: image/avif
----

Keys are the lowercase extension without the dot. An omitted block is the same as an empty one: the
built-in map governs alone.

=== The block is add-only -- and a built-in extension is refused at boot

You can *teach* the gateway an extension it does not know. You cannot *change* one it does. The 21
built-in extensions -- `html`, `htm`, `css`, `js`, `mjs`, `json`, `map`, `xml`, `txt`, `svg`, `png`,
`jpg`, `jpeg`, `gif`, `webp`, `ico`, `woff`, `woff2`, `ttf`, `pdf`, `wasm` -- always win, and an
entry naming any of them makes the gateway *refuse to start*, with the error naming the offending
extension.

That refusal is a security bound, not a limitation to work around. The built-in map is exactly what
makes an asset's `Content-Type` gateway-governed rather than source-dictated. If an entry here could
override one, then anyone who can edit the mounted `gateway.yaml` could remap `svg` away from
`image/svg+xml`, or point any built-in extension at `text/html` -- turning a file served from a
directory mount or a secondary origin into a script-executing document on the *gateway's own
origin*. That is a stored-XSS lever on a security gateway, so the map is closed to overrides while
staying open to additions.

The gateway refuses at boot rather than ignoring the entry at request time deliberately: a silently
ignored mapping looks like a working one in the descriptor, and you would only discover otherwise
from a served response. The boot failure tells you immediately.

*If you believe you need to override a built-in*, the mapping is not the right tool -- serve the
file under an extension the gateway does not map, or correct the file's extension to match its
actual type.
